# Human-Run Test Games Goal Prompt

You are working in `/Users/fluffypro/apps/fmarch`. Follow `AGENTS.md`: assume greenfield/no users, work directly on `main`, prefer the architecturally superior shape over transitional compatibility, keep the slice atomic, and use local proof before committing/pushing.

Current state: the frontend browser acceptance lane is proven by `npm run test:frontend-role-proof:browser`, and `npm run dev:test-game` starts/uses a Rust API plus SvelteKit frontend, seeds one mafiascum D01 game through `/commands`, creates invite-backed browser role URLs, and writes `target/dev-test-game/session.{json,md}` plus `target/dev-test-game/proof-run.json` after `--verify`. The harness now supports friendly named games, default reuse, explicit `--reset` and `--reuse`, port and Postgres preflights, optional browser-entry verification, a machine-checkable proof-run validator, a local ops artifact bundle, a local seed/demo fixture summary, a local identity-adapter proof, a local backup/restore drill, and a local release-readiness checklist that remains explicitly not ready. Treat this as a developer harness, not a release/beta claim.

Goal: finish the remaining work necessary for human-run test games in browser. Make the local path feel like a small product loop: one command starts or reuses the stack, seeds a named playable game, gives clear role entry points, keeps the stack alive, and provides enough cleanup/reseed ergonomics that a human can repeatedly run host/player/admin scenarios without reading smoke-test internals.

Completed build order:

1. `npm run dev:test-game` UX is hardened with idempotent friendly names, default reuse, explicit `--reset` and `--reuse`, API/frontend port checks, and a clear Postgres reachability failure before server startup.
2. `npm run test:dev-test-game-contract` proves argument parsing, session-card shape, seed command plan shape, and invite URL/token output without starting servers.
3. `npm run test:dev-test-game-live` runs the harness with `--reset --verify --no-keepalive`, validates the emitted session and proof-run artifacts, writes a local release-readiness checklist that remains `not_ready`, writes a local ops artifact bundle with redacted role URLs/checksums/counts, writes a local seed/demo fixture summary with redacted role URLs, seeded slots, local demo scenarios, and proof-lane mappings, verifies host/player/action-player/denied-player invite redemption plus capability resolution through the real API/SvelteKit boundary, drives host lock/reject recovery plus Day-to-Night action submission/resolution/advance in the seeded game, proves allowed private-channel post ACK plus denied-channel 403 recovery, and promotes the first multiplayer-hardening lanes with duplicate `command_id` replay, player reconnect recovery, stale player-vote recovery, concurrent vote-race convergence, stale action conflict recovery, and stale host-control recovery.
4. `npm run test:dev-test-game-ops` generates `target/dev-test-game/ops-artifacts.{json,md}` from the current proof-run/readiness artifacts and regenerates readiness with the local ops artifact lane passed while hosted observability/operations stays unproven.
5. `npm run test:dev-test-game-seed-fixture` generates `target/dev-test-game/seed-fixture-summary.{json,md}` from the current session/proof/readiness artifacts; the live and backup/restore gates then regenerate readiness with the local seed/demo fixture lane passed while hosted demo fixtures stay unproven.
6. `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-identity` runs the local invite-role browser proof, proves local session rotation, session revocation, revoked-invite rejection, replacement-invite recovery, and a seeded GlobalAdmin overview-to-audit-detail click-through that renders local lifecycle audit rows over the same role URL architecture, and regenerates readiness with the local identity-adapter lane passed while hosted account lifecycle, invite delivery, account recovery, rate limits, abuse controls, production session-secret policy, and hosted audit retention/export stay unproven.
7. `npm run test:dev-test-game-backup-restore` runs the local dump/restore drill against disposable Postgres databases, validates restored event/projection/session evidence through the real API, generates a backup-aware ops artifact bundle, generates the seed/demo fixture summary, and regenerates the dev-test-game release-readiness checklist with local backup/restore, local ops, and local seed/demo fixture lanes passed while release readiness stays `not_ready`.
8. `docs/ops/human-run-test-games.md` records the exact human command, required Postgres assumption, generated artifact paths, reset/reuse behavior, proof commands, and proof boundary.
9. Rerun the narrow proof set after changing the harness: `node --check tools/dev_test_game.mjs`, `node --check tools/dev_test_game_live_proof.mjs`, `node --check tools/dev_test_game_proof_contract.mjs`, `node --check tools/dev_test_game_release_readiness.mjs`, `node --check tools/dev_test_game_ops_artifacts.mjs`, `node --check tools/dev_test_game_seed_fixture_summary.mjs`, `node --check tools/auth_invite_role_proof.mjs`, `npm run test:dev-test-game-contract`, `npm run test:dev-test-game-proof`, `npm run test:dev-test-game-readiness`, `npm run test:dev-test-game-ops`, `npm run test:dev-test-game-seed-fixture`, `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-identity`, `npm run test:dev-test-game-backup-restore` when backup/restore truth changed, `npm --prefix frontend run check`, `npm run test:frontend-contract`, `npm run test:dev-test-game-live`, and `npm run test:frontend-role-proof:browser` if frontend/browser behavior changed.

Proof boundary to keep honest: this should prove a local seeded browser test-game workflow for one developer, plus the specific duplicate-command, player reconnect, stale player-vote, concurrent vote-race convergence, stale action conflict, stale-host-control, local ops artifact, local seed/demo fixture, local identity-adapter lifecycle/audit shape, and local dump/restore lanes above. It should not claim hosted production account lifecycle, invite delivery, account recovery, rate limits, abuse controls, production session-secret policy, hosted audit retention/export, hosted deployment, hosted demo fixtures or sanitized demo-data policy, production-like backup/PITR, hosted observability/operations, exhaustive race coverage, file upload/transcode, or beta readiness.
