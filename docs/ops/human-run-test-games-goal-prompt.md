# Human-Run Test Games Goal Prompt

You are working in `/Users/fluffypro/apps/fmarch`. Follow `AGENTS.md`: assume greenfield/no users, work directly on `main`, prefer the architecturally superior shape over transitional compatibility, keep the slice atomic, and use local proof before committing/pushing.

Current state: the frontend browser acceptance lane is proven by `npm run test:frontend-role-proof:browser`, and `npm run dev:test-game` starts/uses a Rust API plus SvelteKit frontend, seeds one mafiascum D01 game through `/commands`, creates invite-backed browser role URLs, and writes `target/dev-test-game/session.{json,md}` plus `target/dev-test-game/proof-run.json` after `--verify`. The harness now supports friendly named games, default reuse, explicit `--reset` and `--reuse`, port and Postgres preflights, optional browser-entry verification including a cohost host-console proof with delegated deadline control and host-only command rejection, a machine-checkable proof-run validator, a local ops artifact bundle, a local seed/demo fixture summary, a local identity-adapter proof, a local backup/restore drill, a generated spine manifest, a generated next-action receipt, and a local release-readiness checklist that remains explicitly not ready. Treat this as a developer harness, not a release/beta claim.

Goal: finish the remaining work necessary for human-run test games in browser. Make the local path feel like a small product loop: one command starts or reuses the stack, seeds a named playable game, gives clear role entry points, keeps the stack alive, and provides enough cleanup/reseed ergonomics that a human can repeatedly run host/player/admin scenarios without reading smoke-test internals.

Completed build order:

1. `npm run dev:test-game` UX is hardened with idempotent friendly names, default reuse, explicit `--reset` and `--reuse`, API/frontend port checks, and a clear Postgres reachability failure before server startup.
2. `npm run test:dev-test-game-contract` proves argument parsing, session-card shape, seed command plan shape, and invite URL/token output without starting servers.
3. `npm run test:dev-test-game-live` runs the harness with `--reset --verify --no-keepalive`, validates the emitted session and proof-run artifacts, writes a local release-readiness checklist that remains `not_ready`, runs the backup/restore drill and identity-adapter proof so their upstream artifacts are fresh, then writes `target/dev-test-game/admin-spine-proof.json` by running the ordered core-loop, hardening, identity, backup, ops, seed, release-readiness, and spine-manifest admin browser proofs before a final readiness pass records the aggregate plus per-surface recovery commands without release or production claims. The admin spine also writes `target/dev-test-game/spine-manifest.{json,md}` from the exported proof order, evidence env maps, current aggregate/admin-surface artifact freshness statuses, per-artifact refresh commands sourced from the admin-spine recovery map when available, proof-freshness command/artifact, and terminal next-action artifacts kept separate from freshness inputs, writes `target/dev-test-game/next-action.json` as one machine-readable local recovery or freshness command, writes `target/dev-test-game/next-action-admin-proof.json` by clicking from the seeded admin overview into the native local-next-action detail route, writes `target/dev-test-game/spine-manifest-admin-proof.json` by clicking into the native local-spine-manifest detail route, `target/dev-test-game/admin-spine-admin-proof.json` by clicking into the native local-admin-spine detail route for the aggregate proof and its recovery command summary, and `target/dev-test-game/proof-freshness-admin-proof.json` by clicking into the native local-proof-freshness detail route for the generated artifact freshness dashboard. The underlying live proof still verifies host/player/action-player/denied-player/cohost invite redemption plus capability resolution through the real API/SvelteKit boundary, proves the cohost host-console URL renders only delegated deadline control, can run D01 `ExtendDeadline`, and rejects direct host-only `ResolvePhase` as `NotHost`, drives host lock/reject recovery plus Day-to-Night action submission/resolution/advance in the seeded game, proves the action-player invalid self-action renders a current `InvalidTarget` receipt, refreshes command state, and keeps the legal action available, proves the killed player role URL receives a target-scoped `player_killed` death receipt while normal/action-player URLs do not, proves that same dead role URL has dead command state, disabled vote/post controls, no legal actions, and direct vote/post/action rejects as `SlotNotAlive`, proves the normal player role URL does not expose unowned `factional_kill` and rejects a direct browser `SubmitAction` attempt as `InvalidTarget`, proves allowed private-channel post ACK plus denied-channel 403 recovery, proves host replacement from the seeded host role URL through invalid `ProcessReplacement` recovery and then valid `ProcessReplacement` into a stable slot-history projection, stale outgoing-player `NotYourSlot` recovery with disabled old Slot 7 controls, and incoming `player-rowan` Slot 7 ownership with post/vote plus private-receipt isolation, and promotes the first multiplayer-hardening lanes with duplicate `command_id` replay, player reconnect recovery, stale player-vote recovery, concurrent vote-race convergence, stale action conflict recovery, stale host-control recovery from a frozen host role URL into a visible command-activity receipt plus refreshed current controls, and stale cohost deadline recovery from a frozen delegated role URL into a visible command-activity receipt plus refreshed current delegated controls.
   Current replacement proof also includes `replacement-host-issued-invite`, `replacement-pending-player`, and `replacement-invalid-target-recovery`: the seeded host role URL issues the local `player-rowan` replacement invite before `ProcessReplacement`, the generated URL opens before replacement as an authenticated pending player with no current SlotOccupant authority or player controls, an invalid host browser `ProcessReplacement` with the wrong outgoing user rejects as a visible `InvalidTarget` command-activity receipt while that URL remains pending, and the same URL becomes the `replacementPlayer` role entry used by the incoming-player proof after valid replacement.
4. `npm run test:dev-test-game-ops` generates `target/dev-test-game/ops-artifacts.{json,md}` from the current proof-run/readiness artifacts, runs the seeded admin overview-to-detail browser proof at `target/dev-test-game/ops-admin-proof.json`, and regenerates readiness with the local ops artifact lane passed while hosted observability/operations stays unproven.
5. `npm run test:dev-test-game-seed-fixture` generates `target/dev-test-game/seed-fixture-summary.{json,md}` from the current session/proof/readiness artifacts, runs the seeded admin overview-to-detail browser proof at `target/dev-test-game/seed-admin-proof.json`, and regenerates readiness with the local seed/demo fixture lane passed while hosted demo fixtures stay unproven.
6. `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-identity` runs the local invite-role browser proof, proves local session rotation, session revocation, revoked-invite rejection, replacement-invite recovery, writes `target/dev-test-game/identity-admin-proof.json` from a seeded GlobalAdmin overview-to-local-identity-adapter-detail click-through with lifecycle checks plus admin/host/player role rows, and regenerates readiness with the local identity-adapter lane passed while hosted account lifecycle, invite delivery, account recovery, rate limits, abuse controls, production session-secret policy, and hosted audit retention/export stay unproven.
7. `npm run test:dev-test-game-backup-restore` runs the local dump/restore drill against disposable Postgres databases, validates restored event/projection/session evidence through the real API, generates a backup-aware ops artifact bundle, generates the seed/demo fixture summary, runs the seeded admin overview-to-local-backup-restore-detail browser proof at `target/dev-test-game/backup-admin-proof.json`, and regenerates the dev-test-game release-readiness checklist with local backup/restore, local ops, and local seed/demo fixture lanes passed while release readiness stays `not_ready`.
8. `docs/ops/human-run-test-games.md` records the exact human command, required Postgres assumption, generated artifact paths, reset/reuse behavior, proof commands, and proof boundary.
9. Rerun the narrow proof set after changing the harness: `node --check tools/dev_test_game.mjs`, `node --check tools/dev_test_game_spine_runner.mjs`, `node --check tools/dev_test_game_spine_manifest.mjs`, `node --check tools/dev_test_game_next_action.mjs`, `node --check tools/dev_test_game_next_action_admin_proof.mjs`, `node --check tools/dev_test_game_spine_manifest_admin_proof.mjs`, `node --check tools/dev_test_game_admin_spine_admin_proof.mjs`, `node --check tools/dev_test_game_proof_freshness_admin_proof.mjs`, `node --check tools/dev_test_game_live_spine.mjs`, `node --check tools/dev_test_game_live_proof.mjs`, `node --check tools/dev_test_game_proof_contract.mjs`, `node --check tools/dev_test_game_release_readiness.mjs`, `node --check tools/dev_test_game_ops_artifacts.mjs`, `node --check tools/dev_test_game_seed_fixture_summary.mjs`, `node --check tools/dev_test_game_release_admin_proof.mjs`, `node --check tools/dev_test_game_core_loop_admin_proof.mjs`, `node --check tools/dev_test_game_hardening_admin_proof.mjs`, `node --check tools/dev_test_game_backup_restore_spine.mjs`, `node --check tools/dev_test_game_backup_admin_proof.mjs`, `node --check tools/dev_test_game_identity_spine.mjs`, `node --check tools/dev_test_game_identity_admin_proof.mjs`, `node --check tools/dev_test_game_admin_spine.mjs`, `node --check tools/dev_test_game_admin_spine_proof.mjs`, `node --check tools/auth_invite_role_proof.mjs`, `npm run test:dev-test-game-contract`, `npm run test:dev-test-game-spine-manifest`, `npm run test:dev-test-game-next-action`, `npm run test:dev-test-game-next-action-admin-proof`, `npm run test:dev-test-game-spine-manifest-admin-proof`, `npm run test:dev-test-game-admin-spine-admin-proof`, `npm run test:dev-test-game-proof-freshness-admin-proof`, `npm run test:dev-test-game-proof`, `npm run test:dev-test-game-readiness`, `npm run test:dev-test-game-admin-spine`, `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-live`, `npm --prefix frontend run check`, `npm run test:frontend-contract`, and `npm run test:frontend-role-proof:browser` if frontend/browser behavior changed.

Proof boundary to keep honest: this should prove a local seeded browser test-game workflow for one developer, plus the specific cohost deadline delegation with host-only command rejection, invalid action receipt recovery with refreshed legal controls, target-scoped player death receipt after night resolution, dead-player command-surface recovery with `SlotNotAlive` rejects, player action least-authority rejection for an unowned night action, host replacement, pending replacement-player URL recovery before SlotOccupant authority, invalid replacement `InvalidTarget` host command-activity recovery without granting incoming authority, stale outgoing-player replacement recovery, incoming replacement-player ownership, duplicate-command, player reconnect, stale player-vote, concurrent vote-race convergence, stale action conflict, stale-host-control command-activity recovery, stale cohost deadline command-activity recovery, local ops artifact, local seed/demo fixture, local identity-adapter lifecycle/audit shape, and local dump/restore lanes above. The current replacement boundary includes host-issued local replacement invite generation, pending no-control replacement URL entry before `ProcessReplacement`, rejected invalid replacement that leaves Slot 7 owned by `player-mira`, and the same URL recovering to Slot 7 ownership after valid replacement. It should not claim hosted production account lifecycle, invite delivery, account recovery, rate limits, abuse controls, production session-secret policy, hosted audit retention/export, hosted deployment, hosted demo fixtures or sanitized demo-data policy, production-like backup/PITR, hosted observability/operations, exhaustive race coverage, file upload/transcode, or beta readiness.
