# Human-Run Test Games Goal Prompt

You are working in `/Users/fluffypro/apps/fmarch`. Follow `AGENTS.md`: assume greenfield/no users, work directly on `main`, prefer the architecturally superior shape over transitional compatibility, keep the slice atomic, and use local proof before committing/pushing.

Current state: the frontend browser acceptance lane is proven by `npm run test:frontend-role-proof:browser`, and `npm run dev:test-game` starts/uses a Rust API plus SvelteKit frontend, seeds one mafiascum D01 game through `/commands`, creates invite-backed browser role URLs, and writes `target/dev-test-game/session.{json,md}` plus `target/dev-test-game/proof-run.json` after `--verify`. The harness now supports friendly named games, default reuse, explicit `--reset` and `--reuse`, port and Postgres preflights, optional browser-entry verification including a cohost host-console proof with delegated deadline control and host-only command rejection, a machine-checkable proof-run validator, a local ops artifact bundle, a local seed/demo fixture summary, a local identity-adapter proof, a local backup/restore drill, a generated spine manifest, a generated proof graph with admin-spine coverage enforcement, a generated next-action receipt, and a local release-readiness checklist that remains explicitly not ready. Treat this as a developer harness, not a release/beta claim.

Goal: finish the remaining work necessary for human-run test games in browser. Make the local path feel like a small product loop: one command starts or reuses the stack, seeds a named playable game, gives clear role entry points, keeps the stack alive, and provides enough cleanup/reseed ergonomics that a human can repeatedly run host/player/admin scenarios without reading smoke-test internals.

Completed build order:

1. `npm run dev:test-game` UX is hardened with idempotent friendly names, default reuse, explicit `--reset` and `--reuse`, API/frontend port checks, and a clear Postgres reachability failure before server startup.
2. `npm run test:dev-test-game-contract` proves argument parsing, session-card shape, seed command plan shape, and role credential URL/token output without starting servers.
3. `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-core-live` is the core gameplay live lane: it prebuilds the Rust API, runs the seeded browser live proof, validates the emitted session and proof-run artifacts, runs the core-loop and hardening admin proofs, and writes a local release-readiness checklist that remains `not_ready`. The full `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-live` spine keeps that core proof and then runs seed fixtures, backup/restore, identity, admin, proof graph, next-action, and final readiness. The admin spine also writes `target/dev-test-game/release-admin-proof-contract.json` after the final readiness refresh and records that release admin diagnostics contract as a terminal validation in the admin-spine terminal receipt, spine manifest, and proof graph, writes `target/dev-test-game/spine-manifest.{json,md}` from the exported proof order, evidence env maps, current aggregate/admin-surface artifact freshness statuses, per-artifact refresh commands sourced from the admin-spine recovery map when available, proof-freshness command/artifact, and terminal graph/next-action artifacts, writes `target/dev-test-game/proof-graph.json` as the machine-readable graph of local proof surfaces and recovery edges, writes `target/dev-test-game/proof-graph-admin-proof.json` by clicking from the seeded admin overview into the native local-proof-graph detail route while validating graph coverage against the current admin-spine proof, writes `target/dev-test-game/next-action.json` as one machine-readable local recovery or freshness command, writes `target/dev-test-game/next-action-admin-proof.json` by clicking from the seeded admin overview into the native local-next-action detail route, writes `target/dev-test-game/spine-manifest-admin-proof.json` by clicking into the native local-spine-manifest detail route, `target/dev-test-game/admin-spine-admin-proof.json` by clicking into the native local-admin-spine detail route for the aggregate proof and its recovery command summary, and `target/dev-test-game/proof-freshness-admin-proof.json` by clicking into the native local-proof-freshness detail route for the generated artifact freshness dashboard. The underlying live proof still verifies host/player/action-player/denied-player/cohost invite redemption plus capability resolution through the real API/SvelteKit boundary, proves the cohost host-console URL renders only delegated deadline control, can run D01 `ExtendDeadline`, and rejects direct host-only `ResolvePhase` as `NotHost`, drives host lock/reject recovery plus disposable day-vote lynch resolution and player-clicked no-lynch resolution with visible host/player official outcome panels through host/action-player/target/survivor role URLs, legal Day-to-Night action submission/resolution/advance where the role has an action, and the D04 no-lynch into N04 no-action/D05 transition, proves the seeded host role URL advances locked D01 through the hydrated `Advance by deadline` control with deadline evidence before N01 action submission, proves a stale seeded host role URL rejects that old deadline-advance control with stale deadline-target copy and refreshes to current N01 controls, proves the action-player invalid self-action renders a current `InvalidTarget` receipt, refreshes command state, and keeps the legal action available, proves the killed player role URL receives a target-scoped `player_killed` death receipt while normal/action-player URLs do not, proves that same dead role URL has dead command state, disabled vote/post controls, no legal actions, and direct vote/post/action rejects as `SlotNotAlive`, proves the normal player role URL does not expose unowned `factional_kill` and rejects a direct browser `SubmitAction` attempt as `InvalidTarget`, proves allowed private-channel post ACK plus denied-channel 403 recovery, proves the seeded host role URL publishes a projection-derived D02 official votecount from the hydrated `Publish count` control into the browser/API public thread, proves the seeded host role URL marks Slot 7 dead from the hydrated `Mark dead` control and modkilled from the hydrated `Modkill slot` control, verifies the affected player role URL loses controls with `SlotNotAlive` recovery after each action, and restores Slot 7 alive before replacement continues, proves host replacement from the seeded host role URL through invalid `ProcessReplacement` recovery, valid `ProcessReplacement` into a stable slot-history projection, duplicate successful `ProcessReplacement` `command_id` retry returning the original ACK, stale post-success `ProcessReplacement` recovery with explicit stale-target host conflict copy, stale outgoing-player `NotYourSlot` recovery with disabled old Slot 7 controls, stale outgoing-player action-shaped `SubmitAction` recovery with no stale action controls, stale outgoing-player private-channel recovery with current Rowan private-channel authority, stale outgoing-player private-receipt endpoint recovery with Rowan's current empty private queue, incoming `player-rowan` Slot 7 ownership with post/vote plus private-receipt isolation, replacement-session revocation recovery back to the shared 403 boundary without player controls, fresh replacement-session recovery through seeded account login back to Slot 7 post authority without a browser session grant, stale revoked replacement-session recovery in a separate browser context after the fresh account session exists elsewhere, and fresh replacement-role reconnect recovery back to current Slot 7 state, and promotes the first multiplayer-hardening lanes with replacement-session revocation, refresh, stale revoked-session recovery, replacement reconnect recovery, stale replacement conflict messaging, duplicate replacement/post `command_id` replay, player reconnect recovery, stale player-vote recovery, concurrent vote-race convergence, host lifecycle-control and modkill-control recovery with affected-player disabled controls and restored seed state, concurrent host lifecycle collision recovery with one terminal Slot 7 status, concurrent host complete-game collision recovery with one revealed completed game, stale dead-action conflict recovery with explicit actor-death/current action controls copy, stale action conflict recovery with explicit stale action state/current controls copy, stale host-control recovery from a frozen host role URL into a visible command-activity receipt plus refreshed current controls, stale host resolve recovery from a frozen host role URL into locked D02 controls without ACK stream seqs, stale host advance recovery from a frozen locked-D02 host role URL after live unlock into open D02 controls without ACK stream seqs, stale host publish recovery from a frozen D02 host role URL after live official-votecount publication without appending a duplicate official count, stale host lifecycle recovery from a frozen D02 host role URL after live Mark dead without duplicating the current slot status, stale host modkill recovery from a frozen D02 host role URL after live Modkill slot without duplicating the current slot status, stale host prompt recovery from a frozen local prompt host role URL after live prompt resolution without ACK stream seqs, stale host complete-game recovery, stale player completed-game recovery from a frozen local endgame host role URL after live completion without ACK stream seqs, stale host deadline recovery from a frozen host role URL into refreshed current host phase/deadline controls, and stale cohost deadline recovery from a frozen delegated role URL into a visible command-activity receipt plus refreshed current delegated controls.
   Current replacement proof also includes `replacement-host-issued-invite`, `replacement-pending-player`, `replacement-redeemed-invite-recovery`, `replacement-session-revocation-recovery`, `replacement-session-refresh-recovery`, `replacement-stale-session-after-refresh`, `replacement-reconnect-recovery`, `stale-host-invite-recovery`, `replacement-stale-conflict-message`, `replacement-invalid-target-recovery`, `replacement-idempotent-retry`, and `replacement-stale-success-recovery`: the seeded host role URL issues the local `player-rowan` replacement invite before `ProcessReplacement`, the generated URL opens before replacement as an authenticated pending player with no current SlotOccupant authority or player controls, a fresh browser retry of that redeemed invite URL rejects without creating a session cookie, revoking the active replacement browser session makes the API reject the old cookie and reloads the role path into the shared 403 recovery boundary without player controls, seeded account reauthentication restores the final `replacementPlayer` role URL without replaying the invite or minting a session grant and ACKs a Slot 7 post, a separate stale browser context with the revoked cookie remains on the 403 boundary without player controls after the fresh account session exists elsewhere, the fresh replacement role page drops live projection and reconnects to current Slot 7 command state plus a new Rowan post, a stale host player-invite form rejects the old `player-mira` target after replacement and retries the current `player-rowan` target from the same host surface, an invalid host browser `ProcessReplacement` with the wrong outgoing user rejects as a visible stale-target `InvalidTarget` command-activity receipt while that URL remains pending, the successful replacement `command_id` can be replayed for the original ACK without moving Slot 7, a stale post-success replacement rejects without moving Slot 7 away from Rowan or re-enabling Mira's old URL, and the host-issued URL is the browser entry used by the incoming-player proof after valid replacement.
4. `npm run test:dev-test-game-ops` generates `target/dev-test-game/ops-artifacts.{json,md}` from the current proof-run/readiness artifacts, runs the seeded admin overview-to-detail browser proof at `target/dev-test-game/ops-admin-proof.json`, and regenerates readiness with the local ops artifact lane passed while hosted observability/operations stays unproven.
5. `npm run test:dev-test-game-seed-fixture` generates `target/dev-test-game/seed-fixture-summary.{json,md}` from the current session/proof/readiness artifacts, runs the seeded admin overview-to-detail browser proof at `target/dev-test-game/seed-admin-proof.json`, regenerates readiness with the local seed/demo fixture lane passed while hosted demo fixtures stay unproven, and refreshes `target/dev-test-game/next-action.json` with the same seed evidence so the handoff advances past the recovered local dependency.
6. `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-identity` runs the local invite-role browser proof. It proves Argon2id credential storage, authenticated password rotation from `/auth/account/security?account=<account-id>&returnTo=<role-surface>`, hashed single-use recovery credentials, invalid/expired/revoked/replayed recovery rejection, recovery from `/auth/account/recovery?account=<account-id>&returnTo=<role-surface>`, prior-session revocation, prior-password rejection, and recovered-password return to the unchanged seeded host role URL. Account login, invite redemption, and account recovery share a hashed Postgres credential-attempt scope; the browser drives a visible five-failure lockout with retry timing, verifies hashed scope storage, waits out the local policy, and returns to the same host role URL, while the Rust vertical proves failures combine across all three operations. The lane also proves local session rotation/revocation, revoked-invite rejection, replacement-invite recovery, and host-issued game-scoped local player invite issuance with persisted game scope. It writes `target/dev-test-game/identity-admin-proof.json` from a seeded GlobalAdmin overview-to-local-identity-adapter-detail click-through with lifecycle/delegated-issuance checks plus admin/host/player role rows, and regenerates readiness with the local identity-adapter lane passed while hosted account lifecycle, invite delivery, recovery delivery/traffic, password-parameter monitoring, distributed/edge rate limits and abuse controls, production session-secret policy, and hosted audit retention/export stay unproven. Once the local core, hardening, ops, seed/demo, and identity-adapter rows are passed, `npm run test:dev-test-game-next-action:hosted-identity` refreshes readiness with local capability and identity evidence paths, writes `target/dev-test-game/next-action.json` with the hosted-identity sequence stage, proves the native next-action admin surface, refreshes and proves the final selected next action, selects hosted-identity progression/operator work when that local predicate is stale or missing, and can advance to the next hosted-stage blocker once the hosted-identity local predicate artifacts are current.
7. `npm run test:dev-test-game-backup-restore` runs the local dump/restore drill against disposable Postgres databases, validates restored event/projection/session evidence through the real API, generates a backup-aware ops artifact bundle, generates the seed/demo fixture summary, runs the seeded admin overview-to-local-backup-restore-detail browser proof at `target/dev-test-game/backup-admin-proof.json`, and regenerates the dev-test-game release-readiness checklist with local backup/restore, local ops, and local seed/demo fixture lanes passed while release readiness stays `not_ready`.
8. `docs/ops/human-run-test-games.md` records the exact human command, required Postgres assumption, generated artifact paths, reset/reuse behavior, proof commands, and proof boundary.
9. Rerun the narrow proof set after changing the harness: `node --check tools/dev_test_game.mjs`, `node --check tools/dev_test_game_spine_runner.mjs`, `node --check tools/dev_test_game_spine_manifest.mjs`, `node --check tools/dev_test_game_proof_graph_paths.mjs`, `node --check tools/dev_test_game_proof_graph.mjs`, `node --check tools/dev_test_game_proof_graph_admin_proof.mjs`, `node --check tools/dev_test_game_next_action.mjs`, `node --check tools/dev_test_game_next_action_admin_proof.mjs`, `node --check tools/dev_test_game_spine_manifest_admin_proof.mjs`, `node --check tools/dev_test_game_admin_spine_admin_proof.mjs`, `node --check tools/dev_test_game_proof_freshness_admin_proof.mjs`, `node --check tools/dev_test_game_live_spine.mjs`, `node --check tools/dev_test_game_live_proof.mjs`, `node --check tools/dev_test_game_proof_contract.mjs`, `node --check tools/dev_test_game_release_readiness.mjs`, `node --check tools/dev_test_game_ops_artifacts.mjs`, `node --check tools/dev_test_game_seed_fixture_summary.mjs`, `node --check tools/dev_test_game_release_admin_proof.mjs`, `node --check tools/dev_test_game_core_loop_admin_proof.mjs`, `node --check tools/dev_test_game_hardening_admin_proof.mjs`, `node --check tools/dev_test_game_backup_restore_spine.mjs`, `node --check tools/dev_test_game_backup_admin_proof.mjs`, `node --check tools/dev_test_game_identity_spine.mjs`, `node --check tools/dev_test_game_identity_admin_proof.mjs`, `node --check tools/dev_test_game_admin_spine.mjs`, `node --check tools/dev_test_game_admin_spine_proof.mjs`, `node --check tools/auth_invite_role_proof.mjs`, `npm run test:dev-test-game-contract`, `npm run test:dev-test-game-spine-manifest`, `npm run test:dev-test-game-proof-graph`, `npm run test:dev-test-game-proof-graph-admin-proof`, `npm run test:dev-test-game-next-action`, `npm run test:dev-test-game-next-action-admin-proof`, `npm run test:dev-test-game-spine-manifest-admin-proof`, `npm run test:dev-test-game-admin-spine-admin-proof`, `npm run test:dev-test-game-proof-freshness-admin-proof`, `npm run test:dev-test-game-proof`, `npm run test:dev-test-game-readiness`, `npm run test:dev-test-game-admin-spine`, `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-core-live`, `DATABASE_URL=postgres://fmarch:fmarch@localhost:5544/fmarch npm run test:dev-test-game-live`, `npm --prefix frontend run check`, `npm run test:frontend-contract`, and `npm run test:frontend-role-proof:browser` if frontend/browser behavior changed.

Proof boundary to keep honest: this should prove a local seeded browser test-game workflow for one developer, plus the specific cohost deadline delegation with host-only command rejection, host deadline-evidence phase advance from the seeded role URL, stale deadline-advance recovery with current phase controls, invalid action receipt recovery with refreshed legal controls, target-scoped player death receipt after night resolution, dead-player command-surface recovery with `SlotNotAlive` rejects, player action least-authority rejection for an unowned night action, host official-votecount publication from the seeded role URL, host mark-dead/modkill lifecycle controls with affected-player recovery, host replacement, pending replacement-player URL recovery before SlotOccupant authority, redeemed replacement-invite URL recovery, replacement-session revocation and fresh-session recovery, invalid replacement `InvalidTarget` host command-activity recovery without granting incoming authority, duplicate successful replacement command retry, stale post-success replacement recovery without moving Slot 7 off Rowan, stale outgoing-player replacement recovery, stale outgoing-player action recovery, stale outgoing-player private-channel recovery, stale outgoing-player private-receipt recovery, incoming replacement-player ownership, projection-driven host player-invite retargeting after replacement, stale host player-invite rejection plus current-occupant retry from the same role surface, duplicate post command, player reconnect, stale player-vote, concurrent vote-race convergence, concurrent host lifecycle collision recovery, concurrent host complete-game collision recovery, stale dead-action conflict with explicit actor-death/current action controls copy, stale action conflict with explicit stale action state/current controls copy, stale-host-control command-activity recovery, stale host resolve command-activity recovery, stale host advance command-activity recovery, stale host publish recovery without duplicate official count, stale host lifecycle recovery without duplicate slot-status events, stale host modkill recovery without duplicate slot-status events, stale host prompt recovery without duplicate prompt-resolution events, stale host complete-game recovery, stale player completed-game recovery without duplicate GameCompleted events, stale host deadline command-activity recovery, stale cohost deadline command-activity recovery, local ops artifact, local seed/demo fixture, local identity-adapter lifecycle/audit shape, host-scoped local player invite issuance from the seeded host role URL with persisted game scope, and local dump/restore lanes above. The current replacement boundary includes host-issued local replacement invite generation, pending no-control replacement URL entry before `ProcessReplacement`, fresh-browser rejection for the already-redeemed replacement invite URL, rejected invalid replacement that leaves Slot 7 owned by `player-mira`, valid replacement to `player-rowan`, replay of the same successful replacement `command_id` returning the original ACK without moving Slot 7, stale post-success replacement that keeps Slot 7 with Rowan and Mira's old URL replaced/disabled, the same URL losing Slot 7 private-channel and private-receipt authority after valid replacement while Rowan can still post in that private channel and refresh an empty current private queue, revoked replacement session rejection, and seeded account reauthentication restoring Slot 7 post authority without invite replay or a browser session grant. It should not claim hosted production account lifecycle, invite delivery, account recovery, rate limits, abuse controls, production session-secret policy, hosted audit retention/export, hosted deployment, hosted demo fixtures or sanitized demo-data policy, production-like backup/PITR, hosted observability/operations, exhaustive race coverage, file upload/transcode, or beta readiness.
